{
  "name": "voice-server",
  "version": "1.0.0",
  "description": "A proxy server to get Aliyun voice service token.",
  "main": "server.ts",
  "scripts": {
    "start": "ts-node server.ts",
    "dev": "ts-node --watch server.ts",
    "generate-cert": "node generate-cert.js",
    "start-https": "npm run generate-cert && npm start"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
